<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      text-align: center;
    }

    .card {
      border: 1px solid #ddd;
      border-radius: 8px;
      padding: 20px;
      background-color: #fff;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      margin: 10px;
      width: 300px;
      display: inline-block;
      vertical-align: top;
    }

    .card h2 {
      margin: 0;
      font-size: 18px;
    }

    .card a {
      text-decoration: none;
      color: #007bff;
      font-weight: bold;
    }

    .card a:hover {
      text-decoration: underline;
    }

    .actions {
      margin-top: 20px;
    }

    .actions a,
    .actions form {
      display: inline-block;
      margin: 10px;
    }

    .actions a {
      padding: 10px 20px;
      border: none;
      border-radius: 8px;
      background-color: #007bff;
      color: #fff;
      text-decoration: none;
      font-weight: bold;
      font-size: 16px;
    }

    .actions a:hover {
      background-color: #0056b3;
    }

    .actions form {
      margin: 0;
    }

    .actions button {
      padding: 10px 20px;
      border: none;
      border-radius: 8px;
      background-color: #dc3545;
      color: #fff;
      font-weight: bold;
      font-size: 16px;
      cursor: pointer;
    }

    .actions button:hover {
      background-color: #c82333;
    }

    .notice {
      color: green;
      font-size: 16px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <p class="notice"><%= notice %></p>

    <div class="card">
      <%= render @pessoa %>
    </div>

    <div class="actions">
      <%= link_to "Editar Pessoa", edit_pessoa_path(@pessoa), class: "actions" %>
      <%= link_to "Volta para Pessoas", pessoas_path, class: "actions" %>

      <%= button_to "Excluir Pessoa", @pessoa, method: :delete, class: "actions" %>
    </div>
  </div>
</body>
</html>

