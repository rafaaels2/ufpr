<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      text-align: center;
    }

    .card {
      border: 1px solid #ddd;
      border-radius: 8px;
      padding: 20px;
      background-color: #fff;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      margin: 10px;
      width: 300px;
      display: inline-block;
      vertical-align: top;
    }

    .card h2 {
      margin: 0;
      font-size: 18px;
    }

    .card a {
      text-decoration: none;
      color: #007bff;
      font-weight: bold;
    }

    .card a:hover {
      text-decoration: underline;
    }

    .action-links {
      margin-top: 20px;
    }

    .action-links a {
      display: inline-block;
      margin: 10px;
      padding: 10px 20px;
      border: none;
      border-radius: 8px;
      background-color: #007bff;
      color: #fff;
      text-decoration: none;
      font-weight: bold;
      font-size: 16px;
    }

    .action-links a:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Editando Carro</h1>

    <div class="card">
      <%= render "form", carro: @carro %>
    </div>

    <div class="action-links">
      <%= link_to "Mostar Carro", @carro, class: "card" %> 
      <%= link_to "Volta para Carros", carros_path, class: "action-links" %>
    </div>
  </div>
</body>
</html>

